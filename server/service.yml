apiVersion: v1
kind: Service
metadata:
  name: express-service
spec:
  selector:
    app: express
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 5000
    nodePort: 31110